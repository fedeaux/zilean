.dashboard-component-controls
  .title
    = "{{ dashboard_ctrl.components.#{component}.title }}"

    .actions
      %i.small.close.icon{ 'ng-click': "dashboard_ctrl.toggleComponentVisibility('#{component}')" }
